# Story 13.1 — Remover tempPassword de respostas JSON

**Epic:** E13 — Hardening UI/UX + Segurança Frontend (P0)
**Status:** Draft
**Owner:** @dev (Dex)
**Tipo:** Security fix
**Severidade:** CRÍTICA
**Estimativa:** 0.5 dia

---

## User Story

**Como** super-admin,
**eu quero** que senhas temporárias nunca apareçam em respostas HTTP,
**para que** não haja risco de interceptação ou log de credenciais em plaintext.

---

## Contexto

POST `/api/admin/users` retorna `{ tempPassword: "..." }` no JSON de resposta. Isso viola OWASP A02:2021 (Cryptographic Failures). A senha temporária deve ser comunicada apenas via canal seguro (email).

---

## Acceptance Criteria

### AC1 — API não retorna tempPassword
- [ ] POST `/api/admin/users` NÃO retorna `tempPassword` no body
- [ ] Response retorna `{ ok: true, userId, passwordSentVia: 'email' }`

### AC2 — Envio por email
- [ ] Se email do user existe, envia email com senha temporária
- [ ] Se email não existe, retorna instrução para admin configurar manualmente via Clerk

### AC3 — UI atualizada
- [ ] Componente que cria user mostra "Senha enviada por email" em vez de exibir a senha
- [ ] Fallback: se envio falhou, mostra mensagem "Não foi possível enviar. Configure via painel Clerk."

### AC4 — Testes
- [ ] Test: POST /api/admin/users response não contém campo `tempPassword`
- [ ] Test: POST /api/admin/users response contém `passwordSentVia`

---

## File List

| Arquivo | Mudança |
|---------|---------|
| `src/app/api/admin/users/route.ts` | Remover tempPassword do response |
| Componente UI de criação de user | Atualizar feedback |

---

## Change Log

| Data | Autor | Mudança |
|------|-------|---------|
| 2026-05-21 | @sm (via Orion) | Story criada |
