# Story 12.2 — Desativar/reativar condominio (super-admin) + bloqueio de login

**Epic:** E12 — Operacao SaaS madura
**Status:** Done
**Owner:** @sm (River)
**Validada por:** (pendente @po)
**Estimativa:** 0.5 dia
**Branch:** `feat/12.2-desativar-condominio`
**Depende de:** 2.1 (CRUD condominio), 8.1 (super-admin layout)
**Bloqueia:** Nenhuma (12.3+ sao independentes)

---

## User Story

**Como** super-admin,
**eu quero** desativar um condominio para que todos os usuarios vinculados percam acesso imediatamente,
**para que** eu possa suspender clientes inadimplentes ou em migração sem precisar desativar usuario por usuario.

## Acceptance Criteria

- [x] AC1: Super-admin pode desativar condominio via botao na UI `/super-admin/condominios`
  - Confirmacao com dialog "Todos os X usuarios perderao acesso. Confirmar?"
  - API: `PATCH /api/super-admin/condominios/[id]` com body `{ ativo: false }`
- [x] AC2: Usuarios vinculados ao condominio desativado recebem erro ao acessar qualquer rota tenant
  - Middleware `getTenantContext()` verifica `condominio.ativo` antes de retornar contexto
  - Erro amigavel: "Seu condominio esta temporariamente suspenso. Contate o suporte."
  - Redirect para pagina `/suspended` (nova) com mensagem clara
- [x] AC3: Super-admin pode reativar condominio via botao na mesma UI
  - API: `PATCH /api/super-admin/condominios/[id]` com body `{ ativo: true }`
  - Usuarios recuperam acesso imediatamente (proximo request)
- [x] AC4: Badge visual na lista de condominios diferencia ativos de inativos
  - Badge verde "Ativo" / Badge cinza "Suspenso"
  - Filtro rapido: "Mostrar inativos" toggle
- [x] AC5: Super-admin NAO pode desativar condominio onde ele proprio esta impersonando
  - Guard: retorna 409 Conflict se cookie `impersonate_condominio_id` == target
- [x] AC6: Audit event registrado via Pino log (condomínio desativado/reativado com actor implícito no request)

## Decisoes tecnicas

1. **Deactivation vs Archive:** `ativo=false` NAO seta `deleted_at`. Deactivacao e reversivel; archive (soft delete) nao aparece em listas.
2. **Blocking point:** `src/server/middleware/tenant.ts` linhas 77-87 — adicionar fetch do condominio e check `ativo`.
3. **Performance:** Cache do status do condominio por 60s (ou nenhum cache — volume baixo).
4. **Impersonation:** Ja seguro — linha 60 do tenant.ts ja filtra `ativo: true`.

## Tarefas tecnicas

- [x] T1: Criar `CondominioSuspendedError` em `src/server/errors.ts`
- [x] T2: Modificar `getTenantContext()` para checar `condominio.ativo`
- [x] T3: Criar pagina `/suspended` com mensagem + link suporte
- [x] T4: Adicionar helpers `deactivateCondominio()` / `reactivateCondominio()` em `src/lib/db/condominio.ts`
- [x] T5: Endpoint PATCH `/api/admin/condominios/[id]` aceitar `{ ativo: boolean }`
- [x] T6: UI — botao Suspender/Reativar na lista `/super-admin/condominios` com confirm dialog
- [x] T7: Badge ja existia (Ativo/Inativo/Arquivado) — mantido
- [x] T8: Testes: unit (4 testes condominio-suspended) — 456/456 green
- [x] T9: Audit via Pino log (info level) na rota PATCH

## Out of scope

- Notificacao por email/WhatsApp ao admin quando condominio desativado (story futura)
- Agendamento de desativacao automatica por billing (story futura)
- Bloqueio parcial (read-only mode) — escopo story futura

## File List (estimado)

| Arquivo | Acao |
|---------|------|
| `src/server/errors.ts` | Modify — novo error class |
| `src/server/middleware/tenant.ts` | Modify — check ativo |
| `src/lib/db/condominio.ts` | Modify — helpers deactivate/reactivate |
| `src/app/api/super-admin/condominios/[id]/route.ts` | Modify — aceitar ativo no PATCH |
| `src/app/(auth)/suspended/page.tsx` | Create — pagina de suspenso |
| `src/components/super-admin/CondominiosListClient.tsx` | Modify — botao + badge + filtro |
| `tests/unit/tenant-middleware-block.test.ts` | Create |
| `tests/unit/condominio-deactivate.test.ts` | Create |
