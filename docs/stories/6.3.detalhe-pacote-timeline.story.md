# Story 6.3 — Página `/admin/pacotes/[id]` — detalhe + timeline (FR-063)

**Epic:** E6 — Painel Administrativo (P0)
**Status:** Ready for Review
**Validada por:** @po (Pax) — 2026-05-07 — bundle aprovado
**Owner:** @dev (Dex)
**Estimativa:** 0.5 dia
**Branch sugerida:** `feature/6.x-painel-admin-bundle`
**Depende de:** 6.1

---

## User Story

**Como** admin, **eu quero** abrir cada pacote da lista e ver histórico completo (foto, dados extraídos, eventos), **para que** eu possa auditar tudo (FR-063).

---

## Acceptance Criteria

### AC1 — Página detalhe (server)
- [x] `src/app/(admin)/admin/pacotes/[id]/page.tsx`
- [x] `requireAdmin()` guard
- [x] Carrega pacote + foto + unidade + destinatario + setor + funcionario_recebedor + funcionario_entregador + retirado_por_morador + **eventos ordenados por created_at ASC**
- [x] 404 se pacote não existe / outro tenant

### AC2 — Lib helper
- [x] `src/lib/db/pacote-admin-detail.ts`:
  - `loadPacoteDetail(ctx, id) → PacoteDetail | null`
  - Inclui audit completo

### AC3 — `<PacoteDetalheView>` (server component, sem client logic)
- [x] `src/components/admin/PacoteDetalheView.tsx`
- [x] Layout em 2 colunas (mobile: stacked):
  - **Coluna esquerda:** badge de status + dados textuais (destinatário, unidade, setor, posição, código, remetente, IA confiança)
  - **Coluna direita:** foto + timeline de eventos
- [x] Timeline (`<PacoteTimeline>`):
  - Lista vertical de eventos
  - Cada evento: ícone + tipo (criado/ia_processou/confirmado/retirado/etc) + timestamp + user + metadata resumida
- [x] Botões de ação (no topo):
  - "Resolver pendência" (visível só se status `pendente_identificacao` — implementado na 6.4)
  - Outros ficam para 6.5

### AC4 — Tests
- [x] `tests/integration/pacote-admin-detail.test.ts`:
  - Carrega pacote com audit completo
  - 404 para outro tenant
  - 404 para id inexistente

### AC5 — Quality gates
- [x] Lint/typecheck/test/build OK

## Change Log
| 2026-05-07 | @sm (River) | Draft → Ready |
