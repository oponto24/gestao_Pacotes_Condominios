# Story 8.6 — Admin cadastra outros admins do mesmo cond (`/admin/equipe`)

**Epic:** E8 — Operação SaaS
**Status:** Ready for Review
**Validada por:** @po (Pax) — 2026-05-07 — bundle aprovado
**Owner:** @dev (Dex)
**Estimativa:** 0.25 dia (reusa lib + pattern da 8.5)
**Branch sugerida:** `feature/8.5-7-gestao-users`
**Depende de:** 8.5

---

## User Story

**Como** admin de um condomínio,
**eu quero** adicionar outros admins do meu próprio cond,
**para que** eu não dependa do super-admin Ponto24 toda vez que precisar dar acesso a um sócio síndico, gerente da administradora, etc.

---

## Acceptance Criteria

### AC1 — Página `/admin/equipe`
- [x] Criar `src/app/(admin)/admin/equipe/page.tsx`
- [x] Lista users role=admin do MEU cond — via `AdminUsersListClient.tsx` *(renomeado de `AdminEquipeForm.tsx`)*
- [x] Mostra: email, nome, status (pendente_clerk_link / ativo), data criação
- [x] Adicionar item "Equipe" na sidebar admin

### AC2 — Form adicionar admin (client)
- [x] `src/components/admin/AdminUsersListClient.tsx` *(form + lista consolidados num componente)*
- [x] Inputs: email + nome (cond fixo = atual)
- [x] Submit → POST `/api/admin/users`

### AC3 — API `POST /api/admin/users`
- [x] `src/app/api/admin/users/route.ts`
- [x] Guard: `requireAdmin()` — admin do cond
- [x] Body Zod: `{ email, nome }` (cond_id vem do ctx, não do body)
- [x] Cria User com `role=admin`, `condominio_id=ctx.condominioId`
- [x] Reusa helper `createPendingUser` (8.5)
- [x] Audit log: `acao='admin_created_by_admin'`, `metadata={created_by_admin_id}`

### AC4 — Validação tenant isolation
- [x] Admin do cond A NÃO consegue criar admin no cond B (ctx.condominioId é fixo)
- [x] Test integration cobrindo — `tests/integration/user-management.test.ts`

### AC5 — Tests
- [x] `tests/integration/user-management.test.ts` *(consolidado 8.5/8.6/8.7 — 15 testes)*

### AC6 — Quality gates
- [x] Lint, typecheck, test, build OK

## Change Log
| 2026-05-07 | @sm (River) | Draft → Ready |
