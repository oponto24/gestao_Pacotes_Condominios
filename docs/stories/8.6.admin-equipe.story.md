# Story 8.6 — Admin cadastra outros admins do mesmo cond (`/admin/equipe`)

**Epic:** E8 — Operação SaaS
**Status:** Ready
**Validada por:** @po (Pax) — 2026-05-07 — bundle aprovado
**Owner:** @dev (Dex)
**Estimativa:** 0.25 dia (reusa lib + pattern da 8.5)
**Branch sugerida:** `feature/8.5-7-gestao-users`
**Depende de:** 8.5

---

## User Story

**Como** admin de um condomínio,
**eu quero** adicionar outros admins do meu próprio cond,
**para que** eu não dependa do super-admin Ponto24 toda vez que precisar dar acesso a um sócio síndico, gerente da administradora, etc.

---

## Acceptance Criteria

### AC1 — Página `/admin/equipe`
- [ ] Criar `src/app/(admin)/admin/equipe/page.tsx`
- [ ] Lista users role=admin do MEU cond (filtrado pelo `condominio_id` do `getTenantContext()`)
- [ ] Mostra: email, nome, status (pendente_clerk_link / ativo), data criação
- [ ] Adicionar item "Equipe" na sidebar admin

### AC2 — Form adicionar admin (client)
- [ ] `src/components/admin/AdminEquipeForm.tsx`
- [ ] Inputs: email + nome (cond fixo = atual)
- [ ] Submit → POST `/api/admin/users` (NOVA — distinta do `/api/super-admin/users`)

### AC3 — API `POST /api/admin/users`
- [ ] `src/app/api/admin/users/route.ts`
- [ ] Guard: `requireAdmin()` — admin do cond
- [ ] Body Zod: `{ email, nome }` (cond_id vem do ctx, não do body)
- [ ] Cria User com `role=admin`, `condominio_id=ctx.condominioId`
- [ ] Reusa helper `createPendingUser` (8.5)
- [ ] Audit log: `acao='admin_created_by_admin'`, `metadata={created_by_admin_id}`

### AC4 — Validação tenant isolation
- [ ] Admin do cond A NÃO consegue criar admin no cond B (ctx.condominioId é fixo)
- [ ] Test integration cobrindo

### AC5 — Tests
- [ ] `tests/integration/admin-create-admin.test.ts`:
  - Admin do cond A cria admin → user pendente no cond A
  - Tenta passar `condominio_id` no body → ignorado (tenant isolation)
  - Porteiro tentando chamar → 403
  - Email duplicado → 409

### AC6 — Quality gates
- [ ] Lint, typecheck, test, build OK

## Change Log
| 2026-05-07 | @sm (River) | Draft → Ready |
