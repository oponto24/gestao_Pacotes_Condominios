# Story 11.1 — Modelo Bloco como entidade

**Epic:** E11 — UX admin refinado
**Status:** Done

## Mudanças
- `prisma/schema.prisma`: novo model `Bloco` (id, condominio_id, nome, descricao, ordem, ativo) + `Unidade.bloco_id` FK opcional
- Migration: cria tabela bloco + bloco_id em unidade + **backfill** automático (extrai distinct strings de unidade.bloco existentes, cria registros, vincula FKs)
- `unidade.bloco` (string) mantido como deprecated por 1 release pra rollback seguro
- 3 unit tests Prisma DMMF schema

## Próximos passos (não nesta story)
- 11.2: UI hierárquica `/admin/blocos` (drill down)
- 11.3: renomear menu "Unidades" → "Torres/Blocos"
- 11.4: busca global ⌘K
- 11.5: filtros padronizados

## Validações
- typecheck/lint/build clean, 312/312 unit tests verde
