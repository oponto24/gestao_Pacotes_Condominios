# Story 3.3 — Componente BarcodeScannerInput (html5-qrcode + fallback)

**Epic:** E3 — Chegada do Pacote (componente reusável de UI/UX, consumido em 3.6)
**Status:** Ready
**Validada por:** @po (Pax) — 2026-05-07 — Score 10/10
**Owner:** @dev (Dex)
**Estimativa:** 0.75 dia (html5-qrcode integration + modal fullscreen + fallback texto + tests)
**Branch sugerida:** `feature/3.3-barcode-scanner-input`
**Depende de:** 3.1 (PortariaLayout) — story paralela à 3.2 mas independente

---

## User Story

**Como** porteiro,
**eu quero** bipar o código de barras (1D ou QR) do pacote com a câmera do
celular OU digitar manualmente se o bipe falhar,
**para que** o sistema tenha rastreio único do pacote sem depender só da foto.

---

## Contexto

**FR-011 (PRD):** "Sistema deve permitir bipar o código de barras do pacote
(1D ou QR) usando a câmera do celular — opcional, serve para unicidade e
rastreio."
**UX_SPEC §4.1:** "Bipe ativo: modal full-screen com scanner html5-qrcode,
botão fechar (X)."
**UX_SPEC §3.3 organisms:** "BarcodeScannerInput (câmera + input fallback)"

**Esta story entrega APENAS o componente reusável `<BarcodeScannerInput>`.**
Não implementa a tela `/chegada` completa nem chama API — isso é story 3.6.
Diferença com 3.2: bipe é **opcional** (FR-011), foto é obrigatória (FR-012).

**API esperada:**
```tsx
<BarcodeScannerInput
  value={codigo}
  onChange={(codigo) => setCodigo(codigo)}
  onError={(msg) => /* opcional */}
  placeholder="Código do pacote (opcional)"
/>
```

Retorna **string única** (código lido). Composto: input texto editável + botão "Bipar" que abre modal fullscreen com scanner.

---

## Acceptance Criteria

### AC1 — Componente `<BarcodeScannerInput>` em `src/components/portaria/BarcodeScannerInput.tsx`
- [ ] Client component (`'use client'`)
- [ ] Props: `value: string`, `onChange: (value: string) => void`, `onError?: (msg: string) => void`, `placeholder?: string`, `disabled?: boolean`
- [ ] Layout horizontal: input texto (flex-1) + botão "Bipar" (ícone ScanLine + texto). Em mobile (<sm) podem empilhar
- [ ] Input aceita digitação manual livre (fallback se câmera negada/quebrada)

### AC2 — Modal fullscreen do scanner
- [ ] Click "Bipar" abre `<Sheet>` ou modal custom em modo fullscreen
- [ ] Header do modal: "Bipar código de barras" + botão fechar (X) à direita
- [ ] Visor 4:3 com `<div id="barcode-scanner">` (html5-qrcode renderiza nesse div)
- [ ] Texto auxiliar: "Aponte para o código de barras ou QR Code"
- [ ] Click fora ou X → fecha modal + para scanner

### AC3 — html5-qrcode integration
- [ ] `npm install html5-qrcode` (~50KB minified, sem deps pesadas)
- [ ] Instancia `Html5Qrcode` do `@id`, configura `formatsToSupport`: QR_CODE, CODE_128, CODE_39, EAN_13, EAN_8, UPC_A (cobertura típica de etiquetas Correios + e-commerce BR)
- [ ] `start({ facingMode: 'environment' }, ...)` com fallback para qualquer câmera
- [ ] On detection: chama `onChange(decodedText)` + fecha modal + vibração (50ms)
- [ ] Cleanup explícito: `instance.stop()` + `instance.clear()` no close/unmount

### AC4 — State machine
- [ ] Estados: `closed` (modal fechado), `requesting` (pedindo permissão), `scanning` (scanner ativo), `error`
- [ ] Transições via `useReducer` (mesmo pattern da 3.2)
- [ ] Cleanup garantido em todos os paths (sucesso, erro, close manual, unmount)

### AC5 — Permissão & erros
- [ ] Permissão negada → estado `error` no modal com mensagem "Sem acesso à câmera. Vá em Configurações > Site > Permissões" + botão "Tentar novamente"
- [ ] Sem dispositivo → "Câmera não disponível neste dispositivo"
- [ ] Erro genérico do html5-qrcode → "Falha ao iniciar scanner. Tente digitar o código manualmente."
- [ ] Em qualquer erro, modal continua aberto + dá opção de fechar e digitar

### AC6 — Acessibilidade
- [ ] Botão "Bipar" com `aria-label="Bipar código de barras"`
- [ ] Modal com `role="dialog"` + `aria-modal="true"` + `aria-labelledby` apontando pro título
- [ ] Botão X com `aria-label="Fechar scanner"`
- [ ] Estado `error` com `role="alert"`
- [ ] Foco volta pro input após fechar modal

### AC7 — Suite tests
- [ ] `tests/unit/barcode-scanner-input.test.tsx`:
  - Renderiza input + botão "Bipar"
  - Digitação manual chama onChange
  - Click "Bipar" abre modal (texto "Bipar código de barras" visível)
  - Click X fecha modal
  - Mock html5-qrcode: detecção dispara onChange + fecha modal
  - Mock erro de permissão → modal mostra mensagem + permite digitação
  - Cleanup: scanner.stop() chamado no close/unmount

### AC8 — Documentação
- [ ] Atualizar `docs/runbooks/portaria-pwa.md` com seção "BarcodeScannerInput (story 3.3)":
  - API do componente
  - Formatos suportados (QR + 1D Code 128/39/EAN/UPC)
  - State machine ASCII
  - Limitações: HTTPS obrigatório (igual 3.2), iOS Safari quirks
  - Como testar com QR Code de teste (gerador online)

### AC9 — Quality gates
- [ ] `npm run lint` ✅
- [ ] `npm run typecheck` ✅
- [ ] `npm test` ✅ (176 baseline + ~7 novos)
- [ ] `npm run build` ✅
- [ ] Stack docker continua healthy
- [ ] Smoke E2E manual (em desktop com webcam):
  - **S1:** Click "Bipar" abre modal fullscreen com scanner ativo
  - **S2:** Aponte um QR Code de teste (gere em https://www.qr-code-generator.com/) → detecção, modal fecha, input preenchido com texto do QR
  - **S3:** Digite manualmente "TESTE-123" no input → onChange chamado, valor visível
  - **S4:** Click "Bipar" + X → modal fecha, câmera para (LED off), foco volta no input
  - **S5:** Click "Bipar" + nega permissão → modal mostra erro com retry; pode fechar e digitar manualmente
  - **S6:** Adicionar `<BarcodeScannerInput>` ao playground em `/chegada` (substituindo temporariamente ou adicional ao PhotoCapture) pra testar visual + integração
- [ ] ROADMAP atualizado (3.3 → Ready for Review)

---

## Tasks (sequenciais)

- [ ] **T1.** Branch `feature/3.3-barcode-scanner-input` (a partir de main com 3.2 mergeada)
- [ ] **T2.** Instalar `html5-qrcode` (`npm install html5-qrcode` + adicionar no Dockerfile se aplicável)
- [ ] **T3.** Criar `src/components/portaria/BarcodeScannerInput.tsx` com state machine via useReducer (AC1, AC4)
- [ ] **T4.** Implementar modal fullscreen com `<Sheet>` (já existe da 2.4) ou modal custom (AC2)
- [ ] **T5.** Integrar html5-qrcode com cleanup explícito (AC3) — pattern do 3.2 PhotoCapture
- [ ] **T6.** Tratar erros + fallback de digitação manual (AC5)
- [ ] **T7.** ARIA + acessibilidade (AC6)
- [ ] **T8.** Tests `tests/unit/barcode-scanner-input.test.tsx` com mock de Html5Qrcode (AC7)
- [ ] **T9.** Atualizar `<ChegadaPlaygroundClient>` para incluir BarcodeScannerInput abaixo do PhotoCapture (smoke manual + futura integração 3.6)
- [ ] **T10.** Atualizar `docs/runbooks/portaria-pwa.md` (AC8)
- [ ] **T11.** Smoke HTTP: GET `/chegada` continua 307 sem auth (não regrediu)
- [ ] **T12.** Smoke E2E manual: 6 cenários da AC9
- [ ] **T13.** Quality gates lint/typecheck/test/build
- [ ] **T14.** Commit: `feat(portaria): BarcodeScannerInput component (story 3.3)`
- [ ] **T15.** Atualizar story: ACs/tasks marcadas, Dev Notes, status `Ready for Review`

---

## File List previsto

**Criados:**
- `src/components/portaria/BarcodeScannerInput.tsx`
- `tests/unit/barcode-scanner-input.test.tsx`

**Modificados:**
- `package.json` (+ html5-qrcode)
- `package-lock.json`
- `src/components/portaria/ChegadaPlaygroundClient.tsx` (+ BarcodeScannerInput abaixo de PhotoCapture)
- `docs/runbooks/portaria-pwa.md`
- `docs/stories/ROADMAP.md`
- `docs/stories/3.3.barcode-scanner-input.story.md`

---

## Definition of Done

1. Porteiro consegue bipar QR Code de teste em desktop OU mobile real (HTTPS)
2. Permissão negada NÃO bloqueia: input texto continua funcional pra digitação manual
3. Componente é reusável (consumido em 3.6 sem mudanças)
4. Cleanup correto: html5-qrcode `stop()` + `clear()` em todos paths
5. Modal fecha automaticamente após detecção bem-sucedida (não exige confirmação extra)
6. Tests cobrem caminhos críticos (mock Html5Qrcode + permission denied + cleanup)
7. Story status atualizado para `Ready for Review`

---

## Notas para o @dev

### Decisões pré-aprovadas

1. **`html5-qrcode` direto** (não BarcodeDetector API): BarcodeDetector ainda tem suporte limitado em iOS Safari (até 2025) e nem todos browsers expõem. html5-qrcode é polyfill confiável + ~50KB. Decisão pragmática para MVP.
2. **Modal fullscreen** (não inline): scanner precisa de viewport grande pra detecção confiável de códigos pequenos em etiquetas. Inline fica apertado.
3. **Detecção fecha modal automaticamente:** UX rápida — porteiro não precisa confirmar. Se ele bipou, é porque era o que queria. Pode editar manualmente depois se errado.
4. **Input texto SEMPRE editável** (mesmo após bipe): porteiro pode corrigir/sobrescrever. Não trava em "auto-only".
5. **Formatos suportados:** QR_CODE + CODE_128 + CODE_39 + EAN_13 + EAN_8 + UPC_A. Cobertura de Correios (CODE_128 dominante), e-commerce BR (CODE_128 + EAN), QR (futuro). Excluímos formatos exóticos (CODABAR, ITF) pra reduzir falsos positivos.
6. **Reutiliza `<Sheet>` da 2.4**: já existe componente de drawer fullscreen no projeto. Só usar `side="bottom"` ou full sheet pra UX mobile.
7. **`useReducer` para state machine:** mesmo pattern da 3.2 (consistência) — 4 estados: closed/requesting/scanning/error.
8. **Vibration ao detectar:** mesma microinteraction da 3.2 (50ms).

### Pontos de atenção

1. **HTTPS obrigatório em produção:** mesmo da 3.2 (getUserMedia interno do html5-qrcode).
2. **Cleanup html5-qrcode é EM 2 PASSOS:** `instance.stop()` (para o stream) + `instance.clear()` (limpa o DOM do div). Esquecer um deles vaza memória.
3. **`Html5Qrcode` tem promise-based API:** `start()` retorna Promise — wrap em try/catch + state machine.
4. **iOS Safari pode falhar silenciosamente:** se câmera não inicializa, html5-qrcode às vezes não rejeita a Promise. Adicionar timeout (5s) que dispara erro se scanner não emitir nada.
5. **Detecção pode disparar múltiplas vezes:** debounce ou flag "already detected" pra ignorar callbacks subsequentes do mesmo código.
6. **`formatsToSupport` é array de enum:** importar `Html5QrcodeSupportedFormats` e usar valores explícitos.
7. **Container precisa estar montado quando `start()` chamado:** usar `useRef` + `useEffect` que aguarda mount. Tipicamente faz sense fazer `start()` em `useEffect` que observa `state.kind === 'requesting'`.

### Não fazer agora

- API POST /api/pacotes (envio do código) → 3.4
- Detecção de duplicatas (código já existe no banco) → 3.4 ou 3.10 (validação no backend)
- Suporte a múltiplos códigos no mesmo scan → fora MVP
- Histórico de códigos bipados → fora MVP
- BarcodeDetector API (futuro) → migrar pós-MVP se quiser tirar dep externa
- Customização de cores do scanner overlay → fora MVP

---

## Dev Notes

### Pattern reusado de stories anteriores
- State machine via `useReducer` — pattern PhotoCapture 3.2
- Cleanup determinístico em useEffect — pattern PhotoCapture 3.2
- `<Sheet>` componente — story 2.4 (Sheet do MoradorForm)
- `cn()` helper — utility padrão
- Vibration API — pattern PhotoCapture 3.2

### Nova superfície
- **`html5-qrcode`** — primeira lib de scanning. Adiciona ~50KB ao bundle.
- **`src/components/portaria/`** continua crescendo (3 componentes: PortariaLayout, PhotoCapture, BarcodeScannerInput).
- **Modal fullscreen** com Sheet — primeiro uso bottom/fullscreen no projeto.

### Riscos

1. **Bundle size:** html5-qrcode (~50KB) + lucide icons + componente. Bundle de portaria já tinha 11.6KB com 3.2; com 3.3 vai pra ~70KB+ provavelmente. Aceitável; monitorar se passar de 200KB.
2. **Detecção falha em códigos baixa qualidade:** etiquetas amassadas/borradas podem não bipar. Fallback de digitação manual mitiga.
3. **iOS Safari + Sheet bottom:** keyboard pode esconder input quando aberto. Testar em mobile real.
4. **Race condition entre `start()` e `unmount`:** se user fecha modal rápido após abrir, `start()` ainda em curso. Cleanup com flag `cancelled` (mesmo pattern 3.2).
5. **Múltiplos formatos podem causar lentidão:** quanto mais formatos `formatsToSupport`, mais lento. 6 formatos é equilibrado.

---

## CodeRabbit Integration

- **Pre-commit:** `wsl bash -c '~/.local/bin/coderabbit --prompt-only -t uncommitted'`
- **Foco esperado:** memory leaks (Html5Qrcode cleanup), permission handling, state machine race conditions, acessibilidade do modal
- **Self-healing:** max 2 iterations para CRITICAL

---

## Change Log

| Data | Autor | Mudança |
|---|---|---|
| 2026-05-07 | @sm (River) | Story criada (Draft) |
| 2026-05-07 | @po (Pax) | Validada 10/10 → Status Draft → Ready |

**Sugestões PO para Dev Notes (não-bloqueantes):**

1. **Avaliar `useCameraPermission` hook compartilhado:** PhotoCapture (3.2) e BarcodeScannerInput (3.3) ambos pedem getUserMedia + tratam DOMException names. Se padrão se repetir, refatorar para hook único após 3.3 (tech debt minor).
2. **Debounce/flag `alreadyDetected.current`:** html5-qrcode pode emitir `onDecode` múltiplas vezes para o mesmo código antes do `stop()`. Flag no início do callback evita race entre detection + stop + onChange + close.
3. **`useId()` ou key React no div do scanner:** modal abre/fecha rápido pode causar conflito de "instância já anexada ao div". Gerar ID único previne bug obscuro.
4. **Trim + max-length no input texto:** defesa contra paste acidental de strings gigantes. Não bloqueia bipe real (códigos respeitam padrões), só sanitiza digitação manual.

---

## Próxima ação (cadeia AIOX)

`@po *validate-story-draft 3.3` para 10-point checklist. Se GO → @dev pega.
