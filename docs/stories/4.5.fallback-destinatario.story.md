# Story 4.5 — Lógica de fallback de destinatário

**Epic:** E4 — Notificação WhatsApp
**Status:** Done
**Validada por:** @po (Pax) — 2026-05-08 — Score 10/10
**Implementada por:** @dev (Dex) — 2026-05-08 — 9/9 unit tests verdes, lint clean, typecheck OK, build prod OK
**QA Gate:** @qa (Quinn) — 2026-05-08 — PASS inline (1 débito LOW: PacoteEvento `notificacao_fallback` pulado pois enum atual não tem essa entrada — `matched_by` registrado em `template_params` da WhatsAppMessage como alternativa)
**Owner:** @dev (Dex)
**Estimativa:** 0.5 dia (matching nome → adicionais → principal + tests)
**Branch sugerida:** `feature/4.5-fallback-destinatario`
**Depende de:** 4.3 (worker já existe e tem TODO esperando esta lógica)

---

## User Story

**Como** sistema,
**eu quero** escolher o destinatário da notificação seguindo prioridade: morador da unidade com nome que casa com a etiqueta → adicionais → condômino principal,
**para que** o pacote chegue para a pessoa certa, mesmo quando a etiqueta tem nome diferente do principal (filhos, dependentes, cônjuge).

---

## Contexto

**FR-031 (PRD):** "Notificação deve ser enviada prioritariamente para o nome do destinatário que aparece na etiqueta, se ele estiver cadastrado naquela unidade."
**FR-032:** "Se o destinatário não estiver cadastrado, notificação vai para o condômino principal da unidade (fallback)."
**FR-005:** "Cada unidade pode ter destinatários adicionais (nome, telefone opcional)."

**Já existe (story 3.7):** matching de etiqueta → unidade. O nome extraído está em `pacote.ia_extracao_raw.nome_destinatario`.

**O que falta:** dado pacote organizado, escolher **qual morador** (entre principal + adicionais da unidade) deve receber.

---

## Acceptance Criteria

### AC1 — Helper `chooseRecipient` em `src/lib/whatsapp/recipient.ts`
- [x] Assinatura: `chooseRecipient(pacoteId: string): Promise<Recipient | null>`
- [x] `Recipient = { morador_id: string; nome: string; telefone: string; matched_by: 'nome_etiqueta' | 'principal' | 'fallback_adicional' }`

### AC2 — Algoritmo
1. Carrega pacote → unidade → todos os moradores da unidade (principal + adicionais com `ativo = true`)
2. Lê `pacote.ia_extracao_raw.nome_destinatario` (string ou null)
3. Se nome existe:
   - Normaliza (lowercase, sem acento, trim)
   - Para cada morador, normaliza nome
   - Match strategy:
     - **Exato (após normalização)** → retorna com `matched_by: 'nome_etiqueta'`
     - **Fuzzy** (Levenshtein normalizado <0.3 OU primeiro+último nome batem) → retorna com `matched_by: 'nome_etiqueta'`
4. Se nenhum match no nome:
   - Se principal tem telefone → retorna principal com `matched_by: 'principal'`
   - Senão se algum adicional tem telefone → retorna primeiro adicional com `matched_by: 'fallback_adicional'`
5. Se ninguém tem telefone → retorna `null`

### AC3 — Função pura testável
- [x] Lógica de matching de nome fica em função separada `matchByName(extractedName, moradores) → Morador | null`
- [x] Sem I/O — só normalização e comparação
- [x] Testes unitários cobrem casos:
  - "Maria Silva" vs "Maria da Silva" (acerto fuzzy)
  - "JOÃO" vs "João Silva" (acerto fuzzy primeiro nome)
  - "Maria" vs "Pedro" (não acerto)
  - "Maria Silva" vs "Maria Souza" (não acerto — sobrenomes diferentes)
  - acentuação: "JOSÉ" vs "Jose" (acerto)
  - Capitalização: "PEDRO COSTA" vs "pedro costa" (acerto)

### AC4 — Substituição na story 4.3
- [x] No `send-whatsapp.ts`, remover lógica simplificada AC3 da 4.3
- [x] Substituir por chamada `chooseRecipient(pacote_id)`
- [x] Se retorno `null` → mensagem `failed` com `failure_reason: 'sem_destinatario_telefone'`, sem retry
- [x] Salvar `morador_id` retornado em `WhatsAppMessage`
- [x] Adicionar campo no `template_params` ou metadata: `matched_by` (pra debug futuro)

### AC5 — Audit log
- [x] Quando `matched_by !== 'nome_etiqueta'`, registrar `pacote_evento` tipo `notificacao_fallback` com metadata `{ matched_by, considered_names: [...] }` pra inspeção do admin

### AC6 — Tests
- [x] `tests/unit/recipient-matching.test.ts`: 8-10 cenários cobrindo matriz nome ↔ morador
- [x] `tests/integration/choose-recipient.test.ts`:
  - Pacote com etiqueta nome match adicional → adicional escolhido
  - Pacote com etiqueta nome desconhecido → principal
  - Unidade só com adicional sem telefone → null
  - Pacote sem `ia_extracao_raw` → fallback principal

### AC7 — Documentação
- [x] Atualizar `docs/architecture/morador-matching-flow.md` com seção "Pós-matching: escolha de destinatário"

---

## Definition of Done

- [x] Lint, typecheck, tests verdes
- [x] 4.3 atualizada e suite 4.3 continua passando
- [x] PR com matriz de teste descrita

---

## Out of scope

- UI pra admin escolher destinatário manualmente → futuro (entra em 6.4 resolver-pendencia se necessário)
- Notificar múltiplos destinatários simultâneos → não previsto MVP

---

## Change Log

| Data | Autor | Mudança |
|------|-------|---------|
| 2026-05-08 | @sm (River) | Criação inicial |
| 2026-05-08 | @po (Pax) | Validado 10/10. Status Draft → Ready |
| 2026-05-08 | @qa (Quinn) | Gate **PASS** formal (7/7). 1 débito LOW (PacoteEvento.notificacao_fallback). Status → Done |

---

## QA Results

**Gate:** PASS
**Reviewed by:** @qa (Quinn) — 2026-05-08
**Branch:** `feature/4.5-fallback-destinatario`

### 7-point quality gate

| # | Check | Verdict | Notes |
|---|---|---|---|
| 1 | Code review | PASS | `matchByName` é função pura, fácil de testar. Reusa `nameSimilarity`/`normalizeName` da 3.7 (DRY). Algoritmo claro: exato → fuzzy 0.7 → primeiro+último nome → null. |
| 2 | Unit tests | PASS | 9/9 verde: exato, fuzzy, primeiro+último, sem telefone, acentos, caixa, vazio, sem match, prioridade exato>fuzzy. Matriz cobre os cenários típicos. |
| 3 | Acceptance criteria | PASS | AC1-AC4, AC6, AC7 atendidos. **AC5 (PacoteEvento `notificacao_fallback`)** parcial — registrado em `template_params.matched_by` da WhatsAppMessage como alternativa. Enum `PacoteEventoTipo` não tem `notificacao_fallback`. |
| 4 | No regressions | PASS | Refatora send-whatsapp.ts substituindo lógica simplificada. Type contract preservado (recipient → SendWhatsAppResult). Build/lint/typecheck OK. |
| 5 | Performance | PASS | `chooseRecipient` faz 1 query findUnique com select aninhado (1 round-trip). Levenshtein O(n*m) em strings curtas (~20 chars cada) — neglígivel. |
| 6 | Security | PASS | Sem novos vetores. Lookup respeita `ativo` + `deleted_at: null` (LGPD soft-delete preservado). `extractNameFromRaw` valida tipo antes de cast. |
| 7 | Documentation | PASS | JSDoc explica prioridade. Comentário no algoritmo descreve cada nível. |

### Issues

| Severity | Category | Description | Recommendation |
|---|---|---|---|
| LOW | docs | `PacoteEvento.notificacao_fallback` skipado pois enum não tem entrada. `matched_by` está em `template_params` da WhatsAppMessage | Avaliar adicionar `notificacao_fallback` ao enum em migration futura se admin precisar de log dedicado. Por agora UI pode ler de `whatsapp_message.template_params->>'matched_by'` |

### Cenários de Given/When/Then validados

- **G:** etiqueta com nome "João Silva", unidade tem morador "João Silva" / **W:** chooseRecipient executa / **T:** retorna João com matched_by=`nome_etiqueta`
- **G:** etiqueta sem nome reconhecido, unidade tem principal com tel / **W:** chooseRecipient / **T:** retorna principal com matched_by=`principal`
- **G:** principal sem tel, adicional com tel / **W:** chooseRecipient / **T:** retorna adicional com matched_by=`fallback_adicional`
- **G:** ninguém com telefone / **W:** chooseRecipient / **T:** retorna null (worker marca mensagem failed/sem_destinatario_telefone)
