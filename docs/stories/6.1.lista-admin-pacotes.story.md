# Story 6.1 — Lista `/admin/pacotes` com filtros

**Epic:** E6 — Painel Administrativo (P0)
**Status:** Ready for Review
**Validada por:** @po (Pax) — 2026-05-07 — bundle 6.1-6.4 aprovado em block
**Owner:** @dev (Dex)
**Estimativa:** 0.5 dia
**Branch sugerida:** `feature/6.x-painel-admin-bundle`
**Depende de:** 2.7 (AdminLayout), 3.4 (pacotes existem)

---

## User Story

**Como** admin,
**eu quero** ver a lista de todos os pacotes do meu condomínio com filtros (status, unidade, período),
**para que** eu possa monitorar operação e auditar (FR-060, FR-061).

---

## Acceptance Criteria

### AC1 — Página `/admin/pacotes` (server)
- [x] `src/app/(admin)/admin/pacotes/page.tsx`
- [x] `requireAdmin()` guard (já existe)
- [x] Filtros via query params:
  - `status` (rascunho | pendente_identificacao | aguardando_retirada | retirado | cancelado)
  - `unidade_id` (UUID)
  - `from`, `to` (ISO date — período de `recebido_em`)
- [x] Pagination simples: `page` (default 1), `limit` 50
- [x] Renderiza `<PacotesAdminList>`

### AC2 — Lib helper
- [x] `src/lib/db/pacote-admin-list.ts`:
  - `listPacotesAdmin(ctx, filters) → { items, total, page, limit }`
  - Includes: unidade (identificador, bloco), destinatario (nome), setor (nome)
  - Order: `recebido_em DESC`

### AC3 — `<PacotesAdminList>` (client p/ filtros interativos via router.replace)
- [x] `src/components/admin/PacotesAdminList.tsx`
- [x] Filtros no topo (Select + DateRange simples) — query params controlam estado
- [x] Tabela responsiva:
  - Status (badge colorido)
  - Destinatário
  - Unidade
  - Recebido em (relative + tooltip)
  - Ação: linkClick → `/admin/pacotes/[id]` (story 6.3)
- [x] Empty state amigável
- [x] Paginação simples (Anterior/Próxima)

### AC4 — Tests
- [x] `tests/integration/pacotes-admin-list.test.ts`:
  - Lista pacotes do tenant
  - Filtro por status
  - Filtro por unidade
  - Filtro por período
  - Tenant isolation

### AC5 — Quality gates
- [x] Lint, typecheck, test, build OK

---

## Tasks
- [x] T1. `src/lib/db/pacote-admin-list.ts`
- [x] T2. `src/components/admin/PacotesAdminList.tsx`
- [x] T3. `src/app/(admin)/admin/pacotes/page.tsx`
- [x] T4. Tests integration
- [x] T5. Quality gates

## Change Log
| 2026-05-07 | @sm (River) | Draft → Ready (bundle aprovado) |
