# Story 3.8 — Tela `/chegada/confirmar/[id]` (IAExtractionForm + PATCH confirmar)

**Epic:** E3 — Chegada do Pacote
**Status:** Ready for Review
**Validada por:** @po (Pax) — 2026-05-07 — Score 9.6/10 (ajustes aplicados: schema não exige migração, "outra pessoa" via manual_override)
**Owner:** @dev (Dex)
**Estimativa:** 1.0 dia (form + busca de unidade/morador + endpoint PATCH + tests)
**Branch sugerida:** `feature/3.8-tela-confirmar-ia`
**Depende de:** 3.6 (redirect da CapturaPage), 3.7 (match já populado), 2.2 (Unidade), 2.4 (Morador)

---

## User Story

**Como** porteiro,
**eu quero** revisar e ajustar os dados que a IA extraiu da etiqueta antes de confirmar o pacote,
**para que** mesmo quando a IA errar (FR-014 modo "sempre confirmar"), o cadastro saia certo — destinatário correto vinculado à unidade certa.

---

## Contexto

A 3.6 redireciona pra `/chegada/confirmar/{pacote_id}` após criar o rascunho. A 3.5 + 3.7 já preencheram (no melhor caso): `ia_extracao_raw`, campos textuais (`nome_destinatario_etiqueta`, `cep_etiqueta`, etc.), e — se matching deu certo — `unidade_id` + `destinatario_id`.

**FR-014:** Sempre confirmar antes de gravar. Mesmo com confiança alta, porteiro deve revisar.

**Esta story entrega:**
1. Página server-side `/chegada/confirmar/[id]` que carrega pacote + match + foto
2. Form client `IAExtractionForm` editável (5 campos textuais + unidade + destinatário)
3. Endpoint `PATCH /api/pacotes/{id}/confirmar` que valida + grava + muda status
4. Pipeline de busca de unidade/morador inline (se IA não casou ou user quer trocar)

**Status do pacote após confirmar:** vira `aguardando_retirada` se for caso "matched" + dados completos. Story 3.9 organiza tamanho/setor antes da notificação WhatsApp (4.x). **Decisão:** o status `aguardando_retirada` só é setado **depois** da 3.9 (organizar). Aqui na 3.8 marcamos `confirmado` (criar enum value novo).

**Espera aí:** olhando schema — `PacoteStatus` tem `rascunho | pendente_identificacao | aguardando_retirada | retirado | cancelado`. Não há `confirmado` separado. **Decisão revisada:** após 3.8, status fica `rascunho` mas com flag implícita "pronto para 3.9" (presença de `unidade_id` + `destinatario_id` + `confirmed_em` timestamp). 3.9 promove para `aguardando_retirada` ao definir tamanho/setor.

Adicionar campo `confirmed_em` ao schema (migração pequena) ou usar `pacote_evento` tipo `confirmado` (já existe!) como sinalização. **Decisão final:** usar `pacote_evento` tipo `confirmado` (sem migração de schema), e front-end consulta evento mais recente para saber se já foi confirmado.

**Fluxo:**
1. GET `/chegada/confirmar/{id}` (server component)
2. Carrega pacote + foto principal + dados extraídos + match + unidade.identificador + morador.nome
3. Renderiza `<IAExtractionForm>` com valores pré-preenchidos
4. Porteiro edita campos se necessário; pode buscar outra unidade/morador via combobox
5. Submit → PATCH `/api/pacotes/{id}/confirmar` com payload editado
6. API valida (Zod) + atualiza pacote + cria evento `confirmado` + redireciona pra `/chegada/organizar/{id}` (3.9)
7. Se 3.9 ainda não existe (404) — tudo bem, smoke valida que confirmação salvou no DB

---

## Acceptance Criteria

### AC1 — Página server `/chegada/confirmar/[id]`
- [x] Criar `src/app/(portaria)/chegada/confirmar/[id]/page.tsx`
- [x] `requirePorteiro()` guard (admin ou porteiro)
- [x] Carrega pacote por ID via `withTenant`:
  - Foto principal (storage_path → URL via signed get ou direct path)
  - Campos textuais extraídos (nome, endereço, CEP, complemento, remetente)
  - `ia_confianca` (Decimal)
  - Unidade atual (se vinculada) + Morador destinatário (se vinculado)
  - `destinatario_resolvido_via`
- [x] Verifica que pacote pertence ao mesmo condomínio do user (RLS via withTenant cuida)
- [x] Se pacote já foi confirmado (existe evento `confirmado`), redireciona pra `/chegada/organizar/{id}` (idempotência UX)
- [x] Se pacote não existe ou status `cancelado`/`retirado`, mostra 404 friendly

### AC2 — `<IAExtractionForm>` componente client
- [x] Criar `src/components/portaria/IAExtractionForm.tsx`
- [x] Campos editáveis (text inputs):
  - Nome do destinatário
  - Endereço da etiqueta
  - CEP da etiqueta
  - Complemento
  - Remetente
- [x] Campo "Unidade" — seletor inline no IAExtractionForm *(decisão: sem componente separado `UnidadeSelector.tsx`)*
- [x] Campo "Destinatário" — seletor inline no IAExtractionForm *(decisão: sem componente separado `MoradorSelector.tsx`)*
- [x] Indicador visual da confiança da IA: barra/cor (verde ≥0.8, amarelo 0.5-0.8, vermelho <0.5)
- [x] Banner se `flags.cep_diverge` no metadata: "CEP da etiqueta não bate com o do condomínio"
- [x] Banner se status era `pendente_identificacao`: "IA não conseguiu casar — confirme manualmente"
- [x] Foto thumbnail (clique abre full-size em modal)
- [x] Botão "Confirmar" submit principal (h-12 mobile-friendly)
- [x] Botão "Cancelar" volta pra `/chegada` (descarta edições, pacote fica em rascunho/pendente)

### AC3 — Endpoint `PATCH /api/pacotes/{id}/confirmar`
- [x] Criar `src/app/api/pacotes/[id]/confirmar/route.ts`
- [x] `requirePorteiro()` guard
- [x] Body Zod schema:
  ```ts
  {
    nome_destinatario: string().min(1).max(200),  // sempre obrigatório (sobrescreve nome_destinatario_etiqueta)
    endereco: string().max(500).nullable(),
    cep: string().regex(/^\d{5}-?\d{3}$/).nullable(),
    complemento: string().max(200).nullable(),
    remetente: string().max(200).nullable(),
    unidade_id: string().uuid(),                  // obrigatório
    destinatario_id: string().uuid().nullable(),  // null = "outra pessoa" (usa nome_destinatario)
  }
  ```
- [x] Valida que `unidade_id` pertence ao condomínio do user (consulta DB)
- [x] Se `destinatario_id` informado, valida que pertence à `unidade_id` E ao mesmo condomínio
- [x] Atualiza pacote em transação:
  - **Sobrescreve** `nome_destinatario_etiqueta` com `nome_destinatario` do payload (semântica passa a ser "nome final" — IA original fica em `ia_extracao_raw` para audit)
  - Demais campos textuais editados
  - `unidade_id`, `destinatario_id`
  - `destinatario_resolvido_via` calculado:
    - `destinatario_cadastrado` se `destinatario_id` informado
    - `manual_override` se `destinatario_id` null (terceiro / morador não cadastrado)
  - Cria `pacote_evento` tipo `confirmado` com metadata `{user_id, edited_fields: [...], original_match: {...}}`
- [x] Resposta `{ ok: true, pacote_id }` 200; 400 validação; 404 not found; 409 já confirmado
- [x] Idempotência: se evento `confirmado` já existe, retorna 200 com `already_confirmed: true`

### AC4 — UnidadeSelector + MoradorSelector *(inline no IAExtractionForm)*
- [x] Seletores integrados diretamente no `IAExtractionForm.tsx` *(decisão pragmática: sem componentes separados)*
- [x] Busca unidades e moradores via props server-side (lista completa passada pela page)
- [x] Busca por nome/identificador, suporta keyboard nav, fallback "nenhum encontrado"

### AC5 — Foto thumbnail
- [x] Carrega via signed URL ou direct fetch do storage server-side
- [x] Renderiza `<img>` com `loading="lazy"`, max-w-md, rounded
- [x] Click abre full-size em `<Dialog>` modal

### AC6 — Tests
- [x] `tests/unit/pacote-confirmar-validator.test.ts` *(renomeado de `confirmar-validator.test.ts`)*
- [x] Testes de integração consolidados em `tests/integration/` — 5 cenários:
  - Confirma matched + edita nome → DB atualizado, evento `confirmado` criado
  - Confirma com unidade/destinatário trocados manualmente
  - Falha 404 se pacote de outro tenant
  - Idempotência: 2x PATCH retorna `already_confirmed: true`
  - Falha 400 se unidade_id de outro tenant

### AC7 — Quality gates
- [x] `npm run lint` ✅
- [x] `npm run typecheck` ✅
- [x] `npm test` ✅
- [x] `npm run build` ✅
- [ ] Smoke E2E manual:
  - **S1:** Cadastra unidade+morador → manda foto via `/chegada` → após redirect, edita nome → "Confirmar" → vê redirect (404 da 3.9 OK) → DB tem campos atualizados + evento `confirmado`
  - **S2:** Pacote em `pendente_identificacao` → entra na tela `/chegada/confirmar/{id}` → seleciona unidade+morador manualmente → confirma → DB OK
  - **S3:** Voltar pra `/chegada/confirmar/{id}` já confirmado → vê redirect imediato pra `/chegada/organizar/{id}` (idempotente)

---

## Tasks (sequenciais)

- [x] **T1.** Branch `feature/3.8-tela-confirmar-ia`
- [x] **T2.** Criar `src/app/(portaria)/chegada/confirmar/[id]/page.tsx` (server)
- [x] **T3.** Criar `src/lib/db/pacote-confirmar.ts` — funções `loadPacoteForConfirmar`, `confirmarPacote`
- [x] **T4.** Criar `src/lib/validators/pacote-confirmar.ts` — Zod schema + tests
- [x] **T5.** Criar `src/app/api/pacotes/[id]/confirmar/route.ts` — PATCH handler
- [x] **T6.** Criar `src/components/portaria/IAExtractionForm.tsx` (client)
- [x] **T7.** Seletores unidade/morador inline no IAExtractionForm *(sem componentes separados)*
- [x] **T8.** Tests unit (`pacote-confirmar-validator.test.ts`)
- [x] **T9.** Quality gates (lint, typecheck, test, build)
- [ ] **T10.** Smoke E2E manual (S1-S3) — pendente
- [x] **T11.** Atualizar `docs/runbooks/pacotes-api.md` com novo endpoint
- [x] **T12.** Atualizar ROADMAP

---

## Dev Notes

### Pattern reusado
- `requirePorteiro()` guard (3.4)
- `withTenant` para queries com RLS (2.x)
- Zod validators (3.4, 3.6)
- `pacote_evento` para audit trail (3.4, 3.5, 3.7)

### Nova superfície
- **Primeira página server-side com lookup tenant-scoped via parametro de URL**
- **Combobox shadcn** — pode ser primeira vez no projeto
- **Endpoint PATCH idempotente** com idempotência via evento existente

### Decisões importantes

1. **Sem novo enum status:** evento `confirmado` (já existe em `PacoteEventoTipo`) sinaliza confirmação. Status muda só na 3.9 → `aguardando_retirada`.
2. **Unidade obrigatória no submit:** mesmo que IA tenha errado, porteiro precisa selecionar uma. Se não tem unidade cadastrada, admin precisa cadastrar antes (volta pra 2.2).
3. **"Outra pessoa" como destinatário:** sem campo novo no schema. Se admin escolher "outra pessoa", `destinatario_id` fica `null`, `destinatario_resolvido_via='manual_override'`, e o nome digitado vai pra `nome_destinatario_etiqueta` (semântica passa a ser "nome final do destinatário"). IA original preservada em `ia_extracao_raw` para auditoria.
4. **Sem upload de foto adicional:** foto é só a inicial. Edição de foto fica como tech debt.
5. **Auto-foco quando confiança baixa:** se `ia_confianca < 0.5`, foco inicial no campo Nome (revisão prioritária).

### Riscos

1. **Schema gap:** verificar se `pacote.destinatario_outro_nome` ou similar existe; senão, adicionar campo via migração pequena (ou usar `pacote_evento.metadata` como tech debt).
2. **N+1 queries:** loading inicial pode disparar 4-5 queries (pacote, foto, unidade, morador, eventos). Considerar `select` com includes — Prisma já otimiza, mas medir.
3. **Combobox em mobile:** componentes shadcn `<Command>` podem ter UX ruim em telas pequenas. Fallback: `<select native>` em viewport <640px.
4. **Race condition:** 2 abas abertas confirmando ao mesmo tempo → mitigado pela idempotência (segunda dispara `already_confirmed`).

---

## CodeRabbit Integration

- **Pre-commit:** `~/.local/bin/coderabbit --prompt-only -t uncommitted`
- **Foco esperado:** validação Zod, RLS coverage, idempotência, comboboxes a11y
- **Self-healing:** max 2 iterations para CRITICAL

---

## Change Log

| Data | Autor | Mudança |
|---|---|---|
| 2026-05-07 | @sm (River) | Story criada (Draft) |
| 2026-05-07 | @po (Pax) | Validada 9.6/10. Ajuste: removido `destinatario_outro_nome` (campo não existe no schema); admin escolhendo "outra pessoa" sobrescreve `nome_destinatario_etiqueta` e marca `manual_override`. Status Draft → Ready |

---

## Próxima ação (cadeia AIOX)

`@po *validate-story-draft 3.8` para 10-point checklist. Se GO → @dev pega.
