# Story 8.5 — Super-admin cadastra admin de condomínio (cadastro direto)

**Epic:** E8 — Operação SaaS
**Status:** Ready
**Validada por:** @po (Pax) — 2026-05-07 — bundle 8.5-8.7 aprovado em block
**Owner:** @dev (Dex)
**Estimativa:** 0.5 dia
**Branch sugerida:** `feature/8.5-7-gestao-users`
**Depende de:** 8.1 (super-admin layout), 1.5 (Clerk webhook)

---

## User Story

**Como** super-admin Ponto24,
**eu quero** cadastrar diretamente o primeiro admin de cada condomínio (sem fluxo de convite por email),
**para que** quando eu fechar com um cliente, possa deixar tudo pronto pra o admin entrar via Clerk e operar imediatamente (decisão produto 2026-05-07: cadastro direto).

---

## Contexto

**Decisão de produto:** cadastro direto (não convite por email com link).
- Super-admin digita email + nome + escolhe condomínio → sistema cria registro `User` pendente
- Quando a pessoa cria conta no Clerk com aquele mesmo email, webhook `user.created` reconcilia (já implementado na 1.5/1.10): atualiza `clerk_id` placeholder pelo real, mantém `role` e `condominio_id`

Reuso do pattern do super-admin no seed (`prisma/seed.ts`): `clerk_id='pending_clerk_link_<hash>'` enquanto não houver login.

**Esta story entrega:**
1. Tela `/super-admin/users` listando todos os admins por condomínio
2. Form "Adicionar admin" — escolhe cond + email + nome
3. API `POST /api/super-admin/users` cria User pendente
4. Audit log automático

**Fora do escopo:** porteiros (8.7), admin master vs comum (8.8 backlog), edição/desativação de user (tech debt).

---

## Acceptance Criteria

### AC1 — Página `/super-admin/users` (server)
- [ ] Criar `src/app/super-admin/users/page.tsx`
- [ ] Lista users role=admin do sistema, agrupados por condomínio
- [ ] Mostra: email, nome, status (pendente_clerk_link / ativo), condomínio
- [ ] Adicionar item "Usuários" no `SuperAdminNav`

### AC2 — Form adicionar admin (client)
- [ ] `src/components/super-admin/AdminInviteForm.tsx`
- [ ] Inputs:
  - Condomínio (select dos cond ativos)
  - Email (validação Zod)
  - Nome (max 200 chars)
- [ ] Submit → POST `/api/super-admin/users`
- [ ] Sucesso → toast + refresh lista
- [ ] Bloqueia se email já existe (validação server)

### AC3 — API `POST /api/super-admin/users`
- [ ] `src/app/api/super-admin/users/route.ts`
- [ ] Guard: requer `getTenantContext().kind === 'super_admin'`
- [ ] Body Zod: `{ condominio_id: uuid(), email: string().email(), nome: string().min(1).max(200) }`
- [ ] Valida cond ativo
- [ ] Valida email único na tabela `user`
- [ ] Cria User com:
  - `clerk_id`: `pending_clerk_link_<random_hex_16>` (placeholder único)
  - `role`: `admin`
  - `condominio_id`: do payload
  - `ativo`: true
- [ ] Audit log: `acao='admin_created'`, `entidade_tipo='user'`, `metadata={email, condominio_id}`
- [ ] Retorna 201 com `{ id, email }`

### AC4 — Reconciliação automática
- [ ] Verificar que webhook Clerk `user.created` (já existe em 1.5) faz reconcile por email
- [ ] Test manual: super-admin cria user pendente → pessoa cria conta Clerk com mesmo email → entra direto como admin do cond, sem precisar nova ação

### AC5 — Tests
- [ ] `tests/unit/super-admin-users-validator.test.ts` (8 casos: email válido/inválido, cond_id uuid, etc)
- [ ] `tests/integration/super-admin-create-admin.test.ts`:
  - Super-admin cria → user pendente criado, audit log registrado
  - Admin tentando chamar API → 403
  - Email duplicado → 409
  - Cond inativo → 400

### AC6 — Quality gates
- [ ] Lint, typecheck, test, build OK

---

## Tasks
- [ ] T1. Lib helper `src/lib/db/user-management.ts` — listAdminsByCond + createPendingUser
- [ ] T2. Validator `src/lib/validators/user-create.ts` + tests
- [ ] T3. API `src/app/api/super-admin/users/route.ts`
- [ ] T4. Page `src/app/super-admin/users/page.tsx`
- [ ] T5. Form `AdminInviteForm.tsx` + lista
- [ ] T6. SuperAdminNav: adicionar item Usuários
- [ ] T7. Tests
- [ ] T8. Quality gates

## Change Log
| 2026-05-07 | @sm (River) | Draft → Ready (bundle aprovado) |
