import { CapturaPageClient } from '@/components/portaria/CapturaPageClient';

export const dynamic = 'force-dynamic';
export const runtime = 'nodejs';

export default function ChegadaPage() {
  return <CapturaPageClient />;
}
