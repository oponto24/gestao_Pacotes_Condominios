

# Environment & Secrets (AIOX)
.env
.env.local
.env.*.local
.env.prod
.env.production
*.key
*.pem

# Dependencies (AIOX)
node_modules/
node_modules

# Build & Logs (AIOX)
dist/
build/
*.log
logs/

# IDE & OS (AIOX)
.DS_Store
Thumbs.db
.idea/
*.swp

# AIOX Local (AIOX)
.aiox-core/local/
.claude/settings.local.json
.aiox/install-log.txt

# Next.js / Node (App)
.next/
out/
coverage/
*.tsbuildinfo
next-env.d.ts

# Storage de fotos (volume do app — nunca commitar). Path-anchored com `/`
# pra NÃO ignorar `src/lib/storage/` (código).
/storage/

# Editor caches
.vscode/
*.swo

# clerk configuration (can include secrets)
/.clerk/

# AI tool configs (antigravity, codex, cursor, gemini) — consolidated
.ai-tools/

# Claude Code per-project memory
.claude/projects/

# Fotos de teste local na raiz — não commitar (PII de etiquetas reais).
# Path-anchored com `/` pra NÃO ignorar `modelos_de_etiquetas/*.jpeg` etc.
/pacote_teste*
/*.jpeg
/*.jpg
/*.png
