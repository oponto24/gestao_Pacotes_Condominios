# Dependencies & build outputs (gerados dentro do container)
node_modules
.next
out
coverage
build
dist

# Git e CI
.git
.gitignore
.github
.gitattributes

# Frameworks de IA / agentes (não pertencem à imagem da app)
.aiox-core
.aiox
.claude
.codex
.cursor
.gemini
.antigravity
AGENTS.md

# Documentação (não copiar pra imagem — fica em docs/ no host)
docs
*.md
estudo_etiquetas_e_bipe.md
ideia_de_projeto.md

# Modelos de etiquetas (referência humana, não consumida pelo app)
modelos_de_etiquetas

# Prisma — schema precisa entrar na imagem pro postinstall (prisma generate)
# Migrations e seed.ts ficam excluídos via subpath
prisma/migrations
prisma/seed.ts

# Testes (não vão pra imagem de produção)
tests

# Secrets — nunca, jamais
.env
.env.local
.env.*.local
secrets/
*.key
*.pem

# Storage de fotos (volume runtime)
storage

# IDE / OS
.vscode
.idea
*.swp
*.swo
.DS_Store
Thumbs.db

# Infra (Dockerfile já está dentro de infra/, mas o resto não precisa entrar na imagem)
infra
