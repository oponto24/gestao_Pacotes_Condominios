# Dependencies & build outputs (gerados dentro do container)
node_modules
.next
out
coverage
build
dist

# Git e CI
.git
.gitignore
.github
.gitattributes

# Frameworks de IA / agentes (não pertencem à imagem da app)
.aiox-core
.aiox
.claude
.codex
.cursor
.gemini
.antigravity
AGENTS.md

# Documentação (não copiar pra imagem — fica em docs/ no host)
docs
*.md
estudo_etiquetas_e_bipe.md
ideia_de_projeto.md

# Modelos de etiquetas (referência humana, não consumida pelo app)
modelos_de_etiquetas

# Prisma — schema + migrations precisam entrar na imagem
# (postinstall faz prisma generate; deploy roda prisma migrate deploy)
# seed.ts é opcional — entra para podermos rodar npm run prisma:seed em prod via worker
# (story 3.12 — deploy VPS)

# Testes (não vão pra imagem de produção)
tests

# Secrets — nunca, jamais
.env
.env.local
.env.*.local
.env.prod
.env.production
secrets/
*.key
*.pem

# Storage de fotos (volume runtime)
storage

# IDE / OS
.vscode
.idea
*.swp
*.swo
.DS_Store
Thumbs.db

# Infra (Dockerfile já está dentro de infra/, mas o resto não precisa entrar na imagem)
infra
